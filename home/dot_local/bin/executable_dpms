#!/bin/bash

P=$([ $@ ]&&echo on||echo off)
case $XDG_SESSION_DESKTOP in
	"hyprland") hyprctl dispatch dpms $P;;
	"sway") swaymsg "output * dpms $P";;
	"niri") niri msg action power-${P}-monitors;;
	*) brightnessctl s 0;;
esac
