# Colors
highlight_color = "#{{.col_accent}}"
cursor = "█"

# App launcher
terminal_launcher = "{{.terminal}}"

main_border_color = "#{{.col_nop}}"      # Border color for the main info panel (top)
apps_border_color = "#{{.col_nop}}"      # Border color for the apps list panel (middle)
input_border_color = "#{{.col_nop}}"     # Border color for the input panel (bottom)

# Text Colors (non-highlighted text in each panel)
main_text_color = "#{{.col_fg}}"        # Text color for the main info panel
apps_text_color = "#{{.col_fg}}"        # Text color for the apps list
input_text_color = "#{{.col_fg}}"       # Text color for the input field

fancy_mode = true
title_panel_height_percent = 10

