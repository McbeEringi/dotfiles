#!/usr/bin/bash
case $1 in
	"windows") pkexec sh -c 'efibootmgr -n $(efibootmgr|grep -oP -m1 "Boot\K[\dA-F]{4}(?=\* Windows)")&&reboot';;
	"kexec") pkexec systemctl kexec;;
	*) echo "$0 <windows|kexec>"
esac
